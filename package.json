{
  "name": "batman-list-await-template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean .next": "rmdir /q /s .next",
    "dev": "next dev",
    "build": "next build",
    "export": "next build && next export && type nul > out/.nojekyll",
    "deploy": "yarn export && gh-pages -t -d out",
    "start": "next build && next start"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "cacheable-response": "^1.9.0",
    "next": "9.1.2",
    "react": "16.11.0",
    "react-dom": "16.11.0"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "prettier": "^1.18.2"
  }
}
